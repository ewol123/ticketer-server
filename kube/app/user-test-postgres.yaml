apiVersion: v1
kind: PersistentVolume
metadata:
  name: user-test-postgres-pv
  labels:
    type: local
    app: user-test-postgres
spec:
  storageClassName: manual
  capacity:
    storage: 256Mi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data/invoice"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-test-postgres-pvc
  labels:
    app: user-test-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: user-test-postgres
spec:
  selector:
    app: user-test-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-test-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-test-postgres
  template:
    metadata:
      labels:
        app: user-test-postgres
    spec:
      containers:
        - name: user-test-postgres
          image: postgres
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: user_test
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: test
          volumeMounts:
            - name: storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: user-test-postgres-pvc
      restartPolicy: Always
